<h1>Edit Ads</h1>
 
<%= render 'form' %>
 
<%= link_to 'Back', ads_path %>

<!--
<h1>Editing ads</h1>
 
<%= form_for :ads, url: ads_path(@ads), method: :patch do |f| %>
 
  <% if @ads.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@ads.errors.count, "error") %> prohibited
        this ads from being saved:
      </h2>
      <ul>
        <% @ads.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
 
<% end %>
 
<%= link_to 'Back', adss_path %>

-->